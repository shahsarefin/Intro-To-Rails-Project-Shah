<!DOCTYPE html>
<html lang="en">
  <head>
    <title>AdoptADog-IntroToRailsProjectShah</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <div class="flex-grow-1">
      <%= render 'layouts/menu' %>
      <%= yield %>
    </div>

    <%= render 'layouts/footer' %> <!-- Footer is rendered at the bottom -->
  </body>
</html>
